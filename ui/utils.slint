
import {MenuLabel} from "checkmenu.slint";

export global Utils {
    callback framenum_array(int) -> [string];
    callback str2int(string) -> int;
    callback range(int) -> [int];
    callback frame_menu_labels() -> [MenuLabel];
}
